[global]
    frame_width = 2
    frame_color = "#282a36"
    font = Jetbrains Mono 24
    markup = full
    format = "%s %p\n%b"
    sort = yes
    indicate_hidden = yes
    alignment = left
    bounce_freq = 5
    show_age_threshold = 60
    word_wrap = yes
    ignore_newline = no

    # Size
    shrink = no
    # geometry = "400x200-1000+1200"
    # width = 400
    # height = 200
    # origin = top-center
    # offset = 200x200

    # transparency = 40
    idle_threshold = 120
    monitor = 1
    follow = none
    sticky_history = yes
    history_length = 20
    show_indicators = yes
    line_height = 0
    padding = 20
    # corner_radius = 0
    horizontal_padding = 20
    separator_color = "#282a36"
    separator_height = 2
    startup_notification = false
    dmenu = rofi -dmenu -p dunst
    browser = /usr/bin/google-chrome
    icon_position = left
    icon_path = /usr/share/icons/gnome/16x16/status/:/usr/share/icons/gnome/16x16/devices/
    min_icon_size = 0
    max_icon_size = 128

    title = Dunst
    class = Dunst

    # Progress Bar
    progress_bar = true
    progress_bar_height = 10
    progress_bar_frame_width = 1
    progress_bar_min_width = 150
    progress_bar_max_width = 300

[experimental]
    # Calculate the dpi to use on a per-monitor basis.
    # If this setting is enabled the Xft.dpi value will be ignored and instead
    # dunst will attempt to calculate an appropriate dpi value for each monitor
    # using the resolution and physical size. This might be useful in setups
    # where there are multiple screens with very different dpi values.

    # LOOK: This breaks dunst on the 49" UW so only enable when using on the small screen
    per_monitor_dpi = true

[urgency_low]
    foreground = "#aaaaaa"
    background = "#282a3600"
    frame_color = "#aaeeee"
    timeout = 10

[urgency_normal]
    foreground = "#bd93f9"
    background = "#f1fa8c00"
    frame_color = "#f1fa8c"
    timeout = 20

[urgency_critical]
    foreground = "#f8f8f2"
    background = "#ff555500"
    frame_color = "#ff5555"
    timeout = 0
